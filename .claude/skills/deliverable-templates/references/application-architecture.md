# Application Architecture Template

Generated at Gate G sign-off. Written to `<app>/deliverables/Application-Architecture.md`.
Fill every `{{placeholder}}` from the gate answers. The developer reviews this file and approves before the build starts.

---

```markdown
# Application Architecture: {{App Name}}

> Generated by Intent2App · Gate G sign-off · {{date}}
> Review this document and confirm to start the build. Request changes if anything is wrong.

---

## 1. Overview

| Field | Value |
| --- | --- |
| App Name | {{app-name}} |
| Namespace | {{namespace}} (e.g. `com.company.appname`) |
| App Type | {{cap-service \| cap-fe-lrop \| cap-freestyle \| freestyle-ui5 \| external-fe}} |
| Reference Starter | {{cap-service-only \| cap-fullstack-listreport \| cap-fullstack-freestyle \| freestyle-ui5-ts \| fiori-elements-external-service}} |
| Run Command | {{e.g. `npm run watch-listreportapp`}} |

---

## 2. Requirement Summary

{{Total}} requirements extracted from the FD. {{N}} Designed · {{N}} Deferred · {{N}} Needs-decision.

Full register: `{{app-name}}/deliverables/Requirement-Register.md`

### Headline capabilities (must not be dropped)

- {{REQ-NNN}}: {{one-line description}}
- {{REQ-NNN}}: {{one-line description}}

### Deferred requirements

| REQ-NNN | Description | Reason deferred |
| --- | --- | --- |
| {{REQ-NNN}} | {{description}} | {{reason}} |

### Conflicts resolved

| CONFLICT-NN | Clashing refs | Resolution |
| --- | --- | --- |
| {{CONFLICT-NN}} | {{REQ-NNN / FD-§}} | {{how resolved at which gate}} |

---

## 3. Architecture Decisions

### Gate A — Clean Core
**Delivery model:** Side-by-side BTP/CAP — new app on BTP consuming released OData, zero on-stack modification.

### Gate B — Backend / Data Source
**Decision:** {{Build new CAP service \| Consume existing OData \| No backend \| Add service later}}
{{If consume: Service URL / destination: {{value}}}}
{{If consume: EDMX / $metadata path: {{path}}}}
{{If consume: Annotation strategy: {{backend metadata-extension \| local annotation.xml \| mix}}}}
{{If add later: Backend wiring deferred — placeholder CAP service + mock data scaffolded.}}

### Gate C — Floorplan
**Decision:** {{Fiori Elements — List Report + Object Page \| Freestyle UI5 (TypeScript)}}
**Rationale:** {{why this was chosen over the alternative}}

### Gate D — CAP Scope
**CAP needed:** {{Yes \| No}}
{{If yes:}}
- **Scope (multi-select):** {{New service · Extend existing · Bound actions · Draft on · Events}}
- **Bound actions:** {{list each action + guard, or "none"}}
- **Events:** {{topic + broker, or "none"}}

### Gate E — Auth & Roles
**Auth needed:** {{Yes \| No}}
{{If no: Reason: {{read-only \| prototype \| externally enforced}}}}
{{If yes:}}
- **Model:** {{XSUAA Viewer/Editor/Admin \| Authenticated-user only \| Custom roles \| IAS}}
- **Grant matrix:**

| Entity | Viewer | Editor | Admin |
| --- | --- | --- | --- |
| {{Entity}} | READ | READ, CREATE, UPDATE | READ, CREATE, UPDATE, DELETE |
| {{Entity}} | READ | READ | READ |

### Gate F — Data Model

#### Entities

{{For each entity:}}

**{{EntityName}}**
| Field | Type | Key | Notes |
| --- | --- | --- | --- |
| ID | UUID | ✅ | `cuid` aspect |
| {{field}} | {{CDS type}} | | {{enum values / @Measures / virtual / computed}} |
| createdAt | Timestamp | | `managed` aspect |
| modifiedAt | Timestamp | | `managed` aspect |

#### Associations & Compositions

| From | Type | To | Notes |
| --- | --- | --- | --- |
| {{Entity}} | `Association to` | {{Entity}} | {{cardinality}} |
| {{Entity}} | `Composition of many` | {{Entity}} | {{cascade delete}} |

#### Enums & Value Lists

| Field | Values |
| --- | --- |
| {{field}} | {{value1 \| value2 \| value3}} |

#### Virtual / Computed Fields

| Entity | Field | Type | Computed by |
| --- | --- | --- | --- |
| {{Entity}} | criticality | Integer | `after('READ')` handler — 0 Neutral · 1 Error · 2 Warning · 3 Success |

---

## 4. Build Plan

| Layer | Agent | Output |
| --- | --- | --- |
| CAP backend | `cap-developer` | `{{app-name}}/` — `db/`, `srv/`, `xs-security.json` |
| UI layer | `fiori-developer` | `{{app-name}}/app/` — manifest, annotations, i18n |

**After build:** run `cd {{app-name}} && {{run command}}` to verify the app starts.

---

## 5. Approval

- [ ] Requirements are complete and correctly classified.
- [ ] Architecture decisions (Gates A–F) reflect what was discussed.
- [ ] Data model entities, fields, and associations are correct.
- [ ] Auth grant matrix is correct (or auth is intentionally absent).
- [ ] Deferred requirements are acceptable to defer.

**Approve this document to start the build. Reply with any corrections first.**
```
